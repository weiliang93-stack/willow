// Fill these in after creating a free project at https://supabase.com,
// then run shared/supabase-schema.sql in the project's SQL editor.
// Project Settings > API > Project URL / anon public key.
//
// The anon key is safe to commit — it's meant to be public. Access to
// data is enforced by the Row Level Security policies in
// supabase-schema.sql, not by keeping this key secret.
//
// Until these are filled in, training-app and expense-tracker run in
// local-only mode (same as before) and skip the sign-in screen.
const SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
